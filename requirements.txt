# CI render dependencies for the Materials Genomics manuscript.
# (Replaced the old full-env freeze, which pinned numpy==1.25.1 and conflicted
#  with modern pymatgen/dscribe.) The Jupyter/kernel stack is also installed by
# the publish workflow; the scientific stack below is what the Week-6
# local-atomic-environments notebook imports. No torch/mace required.

jupyter
nbformat
ipykernel

# Scientific stack — pinned to the versions the notebook was authored/verified
# against (all have CPython 3.11 wheels, matching the CI runner).
numpy==1.26.4
scipy==1.17.1
pandas==2.3.0
matplotlib==3.10.3
scikit-learn==1.7.0
pymatgen==2026.5.4
matminer==0.10.1
dscribe==2.1.2
ase==3.26.0
